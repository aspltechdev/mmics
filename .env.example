# Base URL of the backend API, including the /api suffix.
#
# Leave this unset in development and Vite will proxy /api to
# http://localhost:5000 for you (see vite.config.js), which
# avoids CORS entirely while working locally.
VITE_API_URL=http://localhost:5000/api
